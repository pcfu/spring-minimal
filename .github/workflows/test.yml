name: Test

on:
  workflow_dispatch:
    branches:
      - "**"
  push:
    branches:
      - "main"
  pull_request:
    branches:
      - "**"
      - "!main"

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Print greeting
        run: echo "Hello world!"
